class Solution{
public:
    int minimumNumber(int n,vector<int> &arr){
        // Code here
      int min_even=INT_MAX;
      for(int i=0;i<n;i++){
            if(arr[i]%2!=0) return 1;
            else min_even=min(min_even,arr[i]);
        }
      return min_even;
    }
};
